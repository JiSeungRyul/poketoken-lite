# PokeTokenLite — 앱 요약

Claude Code를 쓰면서 실제로 소비한 토큰량을 가지고 포켓몬(1~9세대 전 세대
1025마리 — 151+100+135+107+156+72+88+96+120, 처음부터 전부 부화 후보)을
부화 → 진화 → 졸업시키는 개인용 Windows 트레이 앱.

## 한 줄 컨셉

"일하면(=Claude Code 쓰면) 포켓몬이 자란다." 코딩하는 동안 백그라운드에서
조용히 로그를 읽어서 누적 토큰을 계산하고, 그 양이 쌓일 때마다 알이 부화하고
포켓몬이 진화하고 결국 졸업(도감 등록)한다.

원본 아이디어는 macOS 전용 오픈소스 앱 [PokeTokenBar](https://github.com/chattymin/PokeTokenBar)
(11개 툴 지원, 6개 언어, 풀스펙)에서 가져왔고, 이 프로젝트는 그걸 "1인 개인용,
Windows, Claude Code만" 이라는 최소 스코프로 다시 만든 것. 밸런스/등급 체계
같은 핵심 수치는 레퍼런스 소스코드를 실제로 확인해서 최대한 그대로 가져오되,
막히는 부분(뭘 보여줄지, 어떻게 가지고 놀지)은 직접 쓰면서 나온 요청대로
계속 다듬어가고 있음.

## 핵심 게임 메커니즘

- **토큰 집계**: `~/.claude/projects/**/*.jsonl` 로그를 직접 파싱해서 누적
  토큰 계산(WSL/Windows 로그 폴더 둘 다 자동으로 찾음). 2분마다 자동 갱신.
- **부화**: 알 상태에서 500만 토큰 쌓이면 부화. 진화하지 않는 1단 종만 후보
  (capture_rate 가중치 랜덤 — 희귀할수록 덜 나옴). **첫 부화만** common/uncommon
  으로 제한(첫 포켓몬이 진화 없는 고등급으로 뽑히면 초반 체감이 안 좋아서).
- **등급(티어)**: common / uncommon / epic / legendary / mythical 5단계.
  PokéAPI의 `is_legendary`/`is_mythical`/`capture_rate`로 정확히 판정. 등급은
  **부화 시점에 그 라인 전체에 고정**되고 진화해도 절대 안 바뀜(예: 미진화체가
  common이면 최종진화까지 common).
- **진화/졸업**: 등급별 "졸업까지 총 토큰"(common 1억~mythical 16억, 실측
  사용 패턴 기준으로 "에픽 기준 1주일 정도" 되게 맞춤)을 진화 단계 수에 맞게
  삼각수 가중치로 나눠서 단계별 임계치 계산. 마지막 단계 도달 = 졸업 = 도감 등록.
- **도감**: 졸업한 진화 라인은 통째로(기본형~최종형) 영구 등록되고, **지금
  키우는 중/보관함에 있는 개체가 도달한 단계까지도 실시간으로 발견 처리**돼서
  1025마리 전부가 각자 도감 슬롯이 될 수 있음(중간형만 키워도 그 자체로 발견됨).
  아직 발견 못 한 다음 진화형은 도감에서 `??`로 가려짐.
- **이로치(샤이니)**: 1/64 확률로 부화 시 확정, 진화해도 유지.
- **알 보관함**: 진화/졸업할 때마다 등급 알 1개, **매주 월요일 10시마다 무료
  알 티켓 1개**(common 55%~mythical 2% 가중치)가 적립. 등급 알은 "그 등급
  이상 보장"이지 즉시 부화가 아니라 다시 품어야 함.
- **보관함**: 지금 키우는 애를 언제든 보관하고 새 알을 시작할 수 있음(진화
  없는 종을 뽑아도 다른 애로 갈아탈 수 있게). 보관된 개체는 진행률 그대로
  나중에 다시 꺼내 키울 수 있음.
- **상점 + 가방**: 사용 가능 토큰(누적 토큰 − 상점에서 쓴 토큰, 성장
  진행도와는 무관)으로 이상한 사탕(가방에 보관 후 사용 — 진행도 즉시
  +500만)·이로치 부적(1회성, 확률 영구 2배)·등급 보장 알을 구매. 레퍼런스
  확인 결과 퀘스트는 없어서 상점으로 스코프 좁힘.
- **설정 화면**: 새로고침 주기·부화 가중치 on/off·난이도 배율(0.1~2.0)·
  Windows 알림·시작 시 자동 실행 — 5개.
- **부스트**: 지금 키우는 애를 토글로 "부스팅"하면 진화 진행도가 멈추고
  대신 별도 게이지가 참(1,500만 토큰). 다 채우면 모은 양×1.3배를 이상한
  사탕으로 환전해서 가방에 적립(원할 때 아무 포켓몬한테나 사용 가능).
  게이지 다 차기 전에 취소하면 보상 없이 원래 성장으로 복귀 — 리스크
  요소. 메가진화/기가맥스/테라스탈 본가 컨셉에서 따온 아이디어.

## 화면 구성

- **팝업**: 트레이 클릭(또는 exe 실행 직후) 시 뜨는 메인 창. 지금 키우는 애
  + 진행률 바 + 도감/보관함/알보관함/상점/가방/설정 서브뷰. 도감 행을
  펼치면 진화 라인 + 타입/설명까지 보임. 스프라이트는 애니메이션 GIF,
  클릭하면 통통 튀는 반응, 알일 땐 부화 진행률에 따라 점점 세게 흔들림,
  "다시 키우기" 하면 몬스터볼로 들어갔다 나오는 연출.
- **항상 떠있는 위젯**: 프레임 없는 작은 투명 창 — 등급 색 테두리 + 진행률
  바만 있는 미니 버전. 드래그로 옮길 수 있고, 우클릭 메뉴로 투명도 조절/
  전체 창 전환(더블클릭은 실기에서 불안정해서 뺌). 팝업과 위젯은 항상
  둘 중 하나만 떠있음.
- **트레이**: 몬스터볼 아이콘 고정(포켓몬 스프라이트로 실시간 교체하는 건
  검토했다가 고정으로 가기로 함), 새로고침/위젯 토글/종료 메뉴.

## 기술 스택

Node.js + Electron. 상태는 `userData/state.json`에 저장, 포켓몬 데이터는
빌드 시점에 PokéAPI에서 1회 fetch해서 `data/pokedex.json`으로 캐싱(런타임엔
네트워크 요청 없음). `electron-builder`로 포터블 exe 빌드.

## 현재 상태

핵심 루프(부화→진화→졸업→도감) + 2~9세대(전 세대)·상점+가방·설정 화면까지
전부 실제 Windows 환경에서 동작 확인됨. `npm run dist`로 exe 빌드 가능
(아이콘 포함).

## 관련 문서

- `README.md` — 실행 방법, 파일 구조, 튜닝 포인트.
- `HISTORY.md` — 왜 이렇게 구현했는지 시간순 개발 이력(레퍼런스 확인 결과,
  버그 원인 등).
- `BACKLOG.md` — 다음 단계/아이디어(부스트 기믹 설계 메모 포함).
