# PokeTokenLite — 백로그 / 아이디어

우선순위 낮은 다음 단계와 아직 구현 안 한 아이디어 모음. 이미 끝난 작업의
"왜 이렇게 했는지"는 `HISTORY.md` 참고, 완료된 기능 목록은 `README.md`의
"현재 상태" 참고.

## 메가진화/기가맥스/테라스탈 같은 "부스트" 기믹 (이름 미정)

레퍼런스(PokeTokenBar)엔 없는 기믹, 본가 게임 컨셉에서 따온 사용자
아이디어라 검증된 설계는 아니고 여러 차례 대화로 다듬은 구상 단계 메모.

**기본 규칙(확정):**
- 지금 키우는 컴패니언을 토글로 "부스팅 상태"로 전환.
- **부스팅 중엔 토큰이 평소 진화 진행도(`hatchedAtTotal` 기준)로 안 가고,
  대신 별도의 부스트 게이지로 감** — 즉 부스팅 켜져있는 동안은 진화가
  멈추고 그 대신 게이지가 채워짐(토글 꺼져있을 때만 평소처럼 진화 진행).
- 부스트 게이지를 다 채우면 자동으로 완료되고(부화/진화처럼 별도 "받기"
  버튼 없음) 이상한 사탕으로 환전돼 가방에 적립(아래 "보상 방식" 참고).
- 구현 시 참고: 진행도를 저장된 XP가 아니라 `totalTokens - hatchedAtTotal`
  로 실시간 파생 계산하는 현재 구조상, "부스팅 중 진화 멈춤"은 부스팅
  동안 `hatchedAtTotal`도 토큰 증가분만큼 같이 밀어서 진행도를 그대로
  고정시켜두는 방식으로 구현 가능. 게이지 자체는 별도 앵커
  (`boostStartTotal`)를 하나 더 두고 `totalTokens - boostStartTotal`로
  실시간 계산(다른 진행률들과 같은 패턴). 완료 시엔 `hatchedAtTotal`을
  거꾸로 당기는 캐치업이 아니라, 그냥 밀어주던 걸 멈추고(진화 재개) 모은
  양을 사탕으로 환전 — `applyRareCandy()`는 사탕을 "쓸 때"만 그대로
  재사용됨.

**어떤 종이 어떤 기믹을 쓸 수 있는지 — PokéAPI로 실측 확인(1~1025, 1~9세대
전 세대 기준 — 이제 더 늘어날 일 없음, 다음 세대가 나오기 전까진).**
메가진화/기가맥스는 본가에서도 정해진 종만 가능한 제한적 기믹이라 종
목록이 필요함(테라스탈은 본가에서 종 제한이 없어서 목록 불필요 — 아무
종이나 가능). PokéAPI에 `mega-dimension`이라는 버전그룹으로 라이츄/클레피/
우츠보트/시드라/망나뇽 등 **공식 게임에 없는 팬메이드 메가진화 폼도 섞여
들어있어서**, `version_group`이 실제 공식 게임(`x-y`/
`omega-ruby-alpha-sapphire`, 기가맥스는 `sword-shield`)인 것만 걸러냈다:

- **메가진화 가능 39종** (전부 1~3세대 안에 이미 있음 — 공식 메가진화는
  6세대(ORAS)까지만 나오고 이후 세대엔 아예 없어서 9세대까지 다 추가된
  지금도 이 목록은 그대로): 이상해꽃·리자몽·거북왕·독침붕·피죤투·후딘·
  야도란·팬텀·캥카·쁘사이저·갸라도스·프테라·뮤츠·전룡·강철톤·핫삼·
  헤라크로스·헬가·마기라스·나무킹·번치코·대짱이·가디안·깜까미·입치트·
  보스로라·요가램·썬더볼트·샤크니아·폭타·파비코리·다크펫·앱솔·얼음귀신·
  보만다·메타그로스·라티아스·라티오스·레쿠쟈.
- **기가맥스 가능 30종** (8세대(가라르, 810~905) 추가 후 재확인 완료 —
  칸토 12종 + 갈라르 네이티브 18종): 이상해꽃·리자몽·거북왕·버터플·피카츄·
  나옹·괴력몬·팬텀·킹크랩·라프라스·이브이·잠만보·고릴타·에이스번·
  인텔리레온·아머까오·이올브·갈가부기·석탄산·애프룡·단지래플·사다이사·
  스트린더·다태우지네·브리무음·오롱털·마휘핑·대왕끼리동·두랄루돈·우라오스
  (우라오스는 일격·연격 두 기가맥스 폼이 있지만 종 자체는 1개).
- **테라스탈**: 종 제한 없음 — 지금 있는 1025종(전 세대) 전부 적용 가능.

**보상 방식 — 대화로 합의된 설계(확정, 구현만 남음):**

부스팅한 그 컴패니언한테 자동으로 진행도를 꽂아주는 게 아니라(그러면 "지금
키우는 애"만 득 보고 끝) — **완료 순간 모은 양(× 보너스 배율)을 이상한
사탕 개수로 환전해서 기존 가방(`state.rareCandyCount`)에 그대로 적립**한다.
사용자 요청: "내가 쓰고 싶은 포켓몬한테 쓸 수 있게" — 사탕은 원래 가방에
모아뒀다 아무 때나 지금 키우는 아무 포켓몬한테나 쓸 수 있는 아이템이라(상점
관련 내용은 `HISTORY.md` 참고), 이 요구사항이 자동으로 충족된다. 새 아이템
종류·새 데이터 구조 전혀 안 만들어도 됨 — 상점에서 산 사탕이랑 완전히 같은
풀에 섞여 들어감.

- **환전 계산**: `candies = Math.round((모은 양 × 보너스 배율) / RARE_CANDY_XP)`
  — **반올림**(사용자 확정, 버림 아님). 예: 2900만 모으고 보너스 1.3배면
  3770만 → `Math.round(3770만 / 500만) = 8`개. 컴패니언마다 부스팅해서
  모으는 양이 다 달라도, 이 환전 과정에서 전부 같은 단위(사탕 개수)로
  합쳐지기 때문에 "포켓몬별로 가치가 다른 사탕"이 인벤토리에 따로따로
  쌓이는 일은 없음 — 가방엔 항상 "이상한 사탕 N개"라는 단일 숫자만 있음.
- **부스팅 리스크**: 게이지 다 채우기 전에 토글을 끄면 캐치업/보너스/사탕
  지급 없이 그냥 원래 성장으로 복귀(그동안 멈춰있던 시간은 그대로 손해) —
  끝까지 채워야 이득이 남는 구조.
- 추가로 고민할 지점(아직 미정): 부스팅 중 시각 연출(스프라이트 색조/
  이펙트 등) 넣을지, 반복 사용 가능 여부·쿨다운 유무, 보너스 배율 구체값
  (1.2~1.5배 중 어디), 코스메틱 뱃지(부스트 경험을 도감에 영구 기록)를
  사탕 환전이랑 같이 줄지 별도로 뺄지.

## 5시간/주간 한도 도달 시 "이상한 사탕" 보상 — 보류

한도 정보(`/status`에 뜨는 세션/주간 % 게이지)가 로컬에 저장 안 되고 서버
쪽에서만 계산되는 값이라 실제로 확인함(비공식 API로 흉내 내려면 OAuth
토큰을 가져다 써야 해서 그때 안 하기로 함) — 로컬 로그만으로는 "한도 도달"을
감지할 방법이 없어서 착수 자체가 막혀있음. 나중에 감지 방법이 생기거나(공식
API 등), 순수 토큰 총량 임의 기준으로 근사하는 걸로 타협하면 재검토.

## Codex/Gemini 탭 실제 구현

지금은 팝업에 탭만 있고 "다음 라운드 예정" 플레이스홀더. 실측 확인한 것:
Codex CLI는 `~/.codex/history.jsonl`에 프롬프트 텍스트만 있고 토큰 수는
SQLite(`state_5.sqlite`/`logs_2.sqlite`, `sqlite3` CLI 없어서 python3로
조회)에 있는데 테이블명만 봐선 토큰 컬럼이 바로 안 보여서 스키마 조사가 더
필요함. Gemini CLI는 `~/.gemini/history/`에 프로젝트 경로 포인터만 있고
토큰 사용량 저장 위치를 아직 못 찾음. Claude Code 때처럼 실제 파일/DB
구조를 열어서 확인한 뒤 반영해야 함(추측 파서 금지 — `CLAUDE.md` 원칙).
Codex부터 먼저(SQLite라 스키마 조사가 그나마 직접 가능) 스파이크하고
Gemini는 그 이후로 미루는 걸 추천.

## Electron → 네이티브 전환 검토

Tauri(Rust, popup.html 거의 그대로 재사용 가능·OS 내장 웹뷰라 크로미움
번들 없음)가 WPF/WinUI 3보다 현실적. 지금 Electron 구조의 문제(WSL에
`libnss3` 없어서 실행조차 안 됐던 것 등)가 크로미움 번들 때문 — 급한 건
아님, 당장 안 할 예정. (참고: 항상 떠있는 위젯은 네이티브 전환 기다릴
필요 없이 Electron에서 바로 구현 완료 — `renderer/widget.html`, 트레이
메뉴에서 on/off)

## macOS 빌드 지원

지금 `package.json`의 `electron-builder` 설정엔 `build.win`만 있고
`build.mac`이 없어서 `npm run dist`가 Windows exe만 뽑음. 코드 자체는
Windows 종속적인 부분이 거의 없어서(WSL 로그 스캔만 `win32`일 때만 타는
분기라 맥에선 그냥 안 걸림) 설정 추가 자체는 금방 끝나지만, 트레이 아이콘
모양(다크모드 자동 틴트용 템플릿 이미지 필요할 수 있음)·Dock 아이콘 노출
여부·코드사이닝 안 된 앱의 Gatekeeper 경고 같은 건 실제 맥에서 띄워봐야
확인 가능 — 이 개발 환경엔 맥이 없어서 검증은 사용자가 실제 맥에서 직접
해야 함.
