# PokeTokenLite — 개발 이력

시간순 개발 로그. "뭘 만들었는지"보다 **"왜 그렇게 만들었는지"**를 남기는 게
목적 — 특히 레퍼런스(PokeTokenBar) 소스를 직접 확인해서 내린 판단이나, 실제로
겪은 버그와 그 원인은 다음에 같은 걸 또 조사하지 않아도 되게 최대한 자세히
적어둠. 새 세션이 이 프로젝트를 이어받으면 최신 항목부터 위로 훑는 것보다,
**아래에서 위로(시간순)** 읽는 게 왜 지금 구조가 이런지 이해하기 쉬움.

현재 뭐가 완료/미완료인지는 `README.md`의 "현재 상태", 다음에 할 일은
`BACKLOG.md` 참고. 이 파일은 순수 기록용.

## 초기 MVP

원본 스펙(`CLAUDE.md`)대로 뼈대부터 시작: Claude Code JSONL 로그 스키마
실제로 열어서 확인(`logParser.js`의 `extractUsage()`), Electron IPC 연결
(`preload.js`/`main.js`), 트레이 아이콘, 1세대(151마리) 부화→진화→졸업
기본 루프. 임계치(`HATCH_THRESHOLD` 등)는 전부 추정값으로 시작.

## 위젯 — 드래그/더블클릭 버그 두 번 수정

항상 떠있는 위젯(`renderer/widget.html`) 추가 후 실기(Windows)에서 두 가지
문제 발견:
- **더블클릭이 안 먹음**: `-webkit-app-region: drag`가 더블클릭 감지랑
  충돌. `pointerdown/pointermove/pointerup` + `setPointerCapture`로 직접
  드래그를 구현해서 우회했는데, 그래도 실기에서 더블클릭이 불안정해서
  **더블클릭 자체를 포기**하고 우클릭 메뉴("전체 창 보기")로만 전환하기로
  사용자가 확정. 관련 `open-popup` IPC 채널도 정리.
- **빠르게 드래그하면 마우스만 움직이고 위젯이 안 따라옴**(또는 반대로
  버튼 떼도 계속 따라옴), 브라우저 기본 이미지 드래그가 끼어들어 바탕화면에
  이미지 파일이 생기기도 함: `draggable="false"` + `-webkit-user-drag: none`
  으로 네이티브 이미지 드래그를 죽이고, 커서가 창 밖으로 나가도 이벤트가
  끊기지 않게 Pointer Events + `setPointerCapture`/`releasePointerCapture`로
  전환해서 해결.

## 졸업 연출 버그 — 최종형이 한 번도 안 보이고 바로 알로 바뀜

2단 진화라도 "진화 = 바로 졸업"인 라인(나옹→페르시온 등)에서, `tick()`이
졸업 이벤트가 발생한 바로 그 틱에 `state.companion`을 새 알로 덮어써버려서
팝업/위젯에 최종형이 뜰 틈이 없었음. `state.companion`을 `"graduated"`
상태로 한 틱 남겨뒀다가, **다음** `tick()` 맨 앞에서만 새 알로 넘기도록
수정 — 그 사이 최소 한 번은 최종형이 화면에 보임. `boxCurrentCompanionIfGrowing()`
/`boxAndStartNewEgg()`도 `"graduated"` 상태는 제외하도록 같이 손봄(이미
도감에 영구 등록됐는데 또 보관함에 넣거나 폐기하면 안 되니까).

## 도감 "육성 중" 오분류 버그

사용자가 스크린샷으로 발견: 나옹도 페르시온도 실제로 키운 적 없는데 도감엔
나옹이 "육성 중"으로 표시됨. 원인은 `buildDexAggregate()`가 "졸업한 라인의
일부"와 "진짜 지금 키우는 중"을 구분 안 하고 있었던 것 — 이미 졸업한
나옹→페르시온 라인의 중간 종(나옹)이 영원히 "육성 중"으로 남는 버그. 별도
`isLive` 불리언을 추가해서 `applyLive()`(진짜 `state.companion`/
`storedCompanions`에만 호출)에서만 세팅하도록 분리해서 해결. 같은 스크린샷에서
나온 나머지 3개 질문(두두가 왜 있는지, 알이 왜 있는지, 날짜가 왜 있는지)은
전부 의도된 동작으로 확인됨(버그 아님).

## 보관함 진행률 프리즈 버그 — "라프라스가 0토큰이 됨"

보관함에 넣었다 뺀 컴패니언의 진행률이 사라지는 버그를 겪음(실측: 라프라스
138,365,189토큰 진행분이 0으로 리셋). 원인: `hatchedAtTotal`을 그대로 들고
보관하면, 보관 중에도 전역 `totalTokens`는 계속 늘어서 나중에 꺼낼 때
`progress = totalTokens - hatchedAtTotal` 계산이 보관 기간 동안 쌓인 토큰까지
공짜로 얹어주는 버그가 있었음(이건 그 전에 고쳤던 것). 근데 그 수정 전에 이미
저장된 옛 데이터엔 새로 추가한 `frozenProgress` 필드가 없어서, 꺼낼 때
`?? 0` 폴백이 실제 진행분을 조용히 버림 — `logParser.js`에
`getTotalTokensAsOf(cutoffIso)`(특정 시점까지의 로그만 합산)를 추가하고,
`storedAt` 타임스탬프 기준으로 `frozenProgress`를 역산하는 1회성
마이그레이션(`backfillMissingFrozenProgress()`)으로 실제 저장분을 복구.

## 밸런스 1차 조정 — 졸업 총량 1/7.5배 → 1/5배

레퍼런스(PokeTokenBar) `PokemonBalance.graduationTotal`(실측 하루 평균
2.53억 토큰 기준) 실측값을 확인해서 그대로 가져왔는데 처음엔 이 프로젝트의
실측 사용 패턴(평일 활동일 평균 약 8,150만 토큰/일, 2026-09-08~09-14 기준)
기준 "에픽 = 평일 1주일"이 되게 1/7.5까지 낮췄었음 — 너무 빠르다는 피드백으로
1/5(에픽 = 대략 1.5~3주)로 다시 올림. 이후 이 값 자체는 더 안 건드리고
설정 화면의 난이도 슬라이더로만 페이스를 조절하게 바뀜(아래 "설정 화면" 참고).

## 2세대 추가 → 해금 시스템 도입 → 레퍼런스 확인 후 즉시 폐기

처음엔 "1세대 151/151 다 채워야 2세대 해금" 단계적 잠금(`state.unlockedGen`/
`checkGenUnlock()`/부화 풀의 `maxGen` 필터)으로 구현했음. 그런데 사용자가
"레퍼런스는 다음 세대를 어떻게 처리하는지" 물어봐서 `gh api`로 PokeTokenBar
소스를 직접 확인해보니, 그런 단계적 잠금이 아예 없었음 — `PokemonAssets
.animatedSpeciesIDs = 1...649`(당시 레퍼런스 기준 1~5세대 전체)를 처음부터
하나의 풀로 써서 `capture_rate` 가중치로만 자연스럽게 희귀도를 조절하는
방식. 1세대 151/151엔 전설·환상까지 포함되는데 그거 다 채울 때까지 2세대를
구경도 못 하는 게 캐주얼한 개인용 앱치고 너무 늦게 열린다고 판단해서, 해금
시스템을 전부 걷어내고 레퍼런스 방식(전체 종이 처음부터 다 부화 후보)으로
되돌림 — 이후 3~9세대 추가 때도 이 방식이 그대로 유지됨.

## 도감 전체 보기 + 상세 팝업

- **전체 보기**: 기존엔 발견한 종만 도감에 나왔는데, "전체 다 보여주고 안
  잡은 건 실루엣" 요청으로 `buildPokedexPayload()`를 전 종(`gen1Data`의
  전부) 순회하도록 확장 — 미발견 종은 `discovered:false`만 반환해서
  이름/스프라이트 스포일러 없이 실루엣(`?`)만 보이게 함.
- **상세 팝업(타입/설명)**: 도감 행을 펼치면 기존엔 진화 라인만 보였는데,
  빌드 스크립트가 이미 fetch해두던 PokéAPI 응답(`pokemon.types`,
  `species.flavor_text_entries`)에서 추가 API 호출 없이 타입(18종 한국어
  매핑)/설명 텍스트를 뽑아서 같은 패널에 추가.

## 설정 화면 추가

새로고침 주기·부화 가중치 on/off 백로그에, 대화 중 합의한 3개(난이도 배율,
Windows 자동 실행, 알림 켜기/끄기)를 더해서 총 5개 설정 구현. `growth.js`의
`evaluate()`/`pickHatchSpecies()`를 계속 늘어나는 위치 인자 대신 옵션 객체
(`{ firstHatch, useWeighting, difficulty }`)로 리팩터. 고정 `setInterval`
대신 자기재귀 `setTimeout`(`scheduleNextTick()`)으로 바꿔서 새로고침 주기가
바뀌면 코드 재시작 없이 바로 반영되게 함. `difficulty` 배율은 `GRADUATION_TOTAL`/
`HATCH_THRESHOLD` 상수표 자체를 안 건드리고 소비 지점(`stageThresholds()`)에서만
곱함(레퍼런스 `PokemonBalance.scaled()`와 동일 원칙).

## 상점 + 가방 — 완료

백로그 "알 구매/퀘스트/보상 시스템"을 레퍼런스(PokeTokenBar) `ShopView.swift`/
`CompanionStore.swift`로 직접 확인해보니 **퀘스트는 레퍼런스에 없어서**(관련
파일 자체가 없음) "상점"으로 스코프를 좁혔다. 민트(성격 리롤)는 우리가 성격
시스템 자체가 없어서 스킵.

재화(`spendableTokens` = 누적 토큰 − `state.tokensSpent`)는 성장 진행도
(`hatchedAtTotal` 기준 실시간 파생값)와 완전히 독립 — 상점에서 아무리 사도
지금 키우는 개체의 진화 속도엔 영향이 없다(레퍼런스 `availableTokens =
usedSinceInstall - spentTokens`와 동일 원칙). 이 앱은 저장된 XP 누적치가
없고 진행도를 항상 `totalTokens - hatchedAtTotal`로 실시간 계산하기 때문에,
이상한 사탕의 "XP를 더한다" 효과는 곧 `hatchedAtTotal`(알이면 `eggStartTotal`)을
과거로 당기는 것과 동치다(`resumeStoredCompanion()`의 `frozenProgress` 역산과
같은 패턴 — `growth.js`의 `applyRareCandy()`).

판매 아이템: 이상한 사탕(가방에 보관 후 사용 — 사용 시 진행도 즉시 +500만,
`HATCH_THRESHOLD`와 동급), 이로치 부적(1회성, 보유하면 이로치 확률 영구 2배),
등급 보장 알(가격은 `GRADUATION_TOTAL[tier]`의 20% — 레퍼런스는 구매 즉시
현재 개체를 폐기+교체하지만, 우리는 기존 "알 보관함 → 품기 시작" 경로가 더
단순하고 일관적이라 그대로 재사용).

## 2~9세대 추가(전 세대 완료)

도감 등록 구조(졸업한 라인 전체 영구 등록 + 성장 중/보관함 진행분 실시간
발견)는 이전에 먼저 해결해서 151/151이 구조적으로 달성 가능해져 있었고,
실제 2세대 데이터를 붙였다.

**세대별 독립 범위 빌드**: PokéAPI의 evolution-chain은 세대 구분이 없어서(예:
이브이(1세대)의 진화형이 2·4·6세대에 걸쳐있음) 직접 확인 후 `flattenChain()`을
`(minId, maxId)` 범위를 받게 일반화해서, **각 세대를 서로 완전히 독립된
범위로 클리핑**해 빌드한다(`scripts/build-pokedex-data.js`). 그래서
폴리곤(1세대)→폴리곤2(2세대)처럼 세대를 넘나드는 진화는 각 세대 파일에서
독립된 별개의 종으로 취급됨(1세대 데이터의 피츄→피카츄 관계가 이미 이렇게
끊겨있던 것과 같은 패턴). 결과는 `data/pokedex.json` 하나에 전 세대가 다
들어있고, 각 항목에 `generation` 필드로 구분(게임 로직에서 이 필드는 안 씀).

**3세대(호연, 252~386) 추가**: 위 방식이 세대 수와 무관하게 일반화돼
있어서, `GENERATIONS` 배열에 `{ gen: 3, min: 252, max: 386 }` 한 줄만
추가하고 재빌드하는 것으로 끝났다 — 게임 로직 쪽은 전혀 안 건드림.
크로스세대 진화 클리핑도 그대로 잘 작동함을 확인(루리리(3세대 #298)→
마릴(2세대 #183)은 피츄→피카츄 케이스와 똑같이 루리리가 독립된 1단 종으로
분리됨). 레쿠쟈(전설), 지라치·테오키스(환상) 등급도 `is_legendary`/
`is_mythical` 플래그로 정상 분류 확인.

**4세대(신오, 387~493) 추가 — "샌드위치 체인" 버그 발견/수정**: 배열에 한
줄 추가하고 재빌드하는 것까진 3세대 때와 동일했는데, 빌드 결과를
스팟체크하다가 새로운 종류의 버그를 하나 발견했다 — 꼬몽울(4세대 #406)→
로젤리아(3세대 #315)→로즈레이드(4세대 #407)처럼 **양 끝은 같은 세대인데
중간만 다른 세대인 "샌드위치" 체인**에서, 4세대 빌드가 로젤리아를
건너뛰면서도 그 이후 노드(로즈레이드)에 "범위 안 조상까지의 단계 수"를
그대로 물려주는 바람에 로즈레이드가 stage:2로, 같은 체인 캐시를 공유하는
꼬몽울의 maxStage까지 2로 잘못 올라가는 문제였다. 근데 evolvesTo 엣지는
원본 트리에서 직접 인접한 노드끼리만 잇기 때문에(중간에 범위 밖 노드가
끼면 간접 연결 안 함) 꼬몽울→로즈레이드 엣지는 끝내 안 생겨서, 꼬몽울이
"2단계인데 다음 단계로 갈 evolvesTo가 없는" 모순 상태가 됐다 — 실제로
키우면 `evaluate()`가 다음 종을 못 찾아 자기 자신으로 폴백해서, 겉보기엔
진화/졸업 이벤트가 뜨는데 스프라이트는 안 바뀌는 버그로 이어졌을 것. 전체
종을 "stage < maxStage인데 evolvesTo가 비어있는" 조건으로 스캔해서 이
패턴에 해당하는 게 꼬몽울 하나뿐임을 확인하고, `flattenChain()`이 범위
밖 노드를 지날 때 `relativeStage`를 이어받지 않고 무조건 `undefined`로
리셋하도록 한 줄 고쳤다 — 이러면 꼬몽울·로즈레이드가 루리리→마릴처럼
완전히 독립된 1단(stage:1, maxStage:1) 종으로 정직하게 분리된다. 이미
검증된 2단 크로스세대 케이스(피츄→피카츄, 루리리→마릴)는 애초에 범위 밖
노드를 한 번만 거치는 구조라 이 수정으로 동작이 안 바뀌는 것도 재확인.

**5~9세대(하나 494~649·칼로스 650~721·알로라 722~809·가라르 810~905·
팔데아 906~1025) 추가**: 전부 배열에 한 줄씩 추가+재빌드, 매번 전체 종
stage/maxStage/evolvesTo 일관성 스윕("샌드위치 체인" 체크 포함) 통과,
특이사항 없음. 8세대 추가로 갈라르 네이티브 기가맥스 18종이 새로 생겨서
"부스트" 기믹 백로그의 기가맥스 목록을 12종→30종으로 갱신함(`BACKLOG.md`
참고). 9세대는 메가진화/기가맥스 둘 다 추가 없음 — Scarlet/Violet은
다이맥스/기가맥스 자체가 없는 세대(테라스탈만 있음)라 예상대로였고,
PokéAPI에 9세대 종 몇 개에 "mega" variety가 붙어있던 건 확인해보니 전부
`mega-dimension`(팬메이드, 4세대 항목에서도 걸러냈던 것과 같은 비공식
버전그룹)이라 제외.

**1~9세대(전 세대) 완료 — 총 1025마리.** 다음 세대가 새로 나오기 전까진
추가할 게 없음.

## 알 사라짐 버그 수정

사용자 리포트: "알 선택하고 키우다가 부화 안 하고 다시 라프라스 키우기로
넘어갔는데 알이 사라짐". 원인: `resumeStoredCompanion()`("보관함 → 다시
키우기")이 `boxCurrentCompanionIfGrowing()`만 호출했는데, 이 함수는 이름
그대로 `companion.state === "growing"`일 때만 보관 처리를 하고 `"egg"`
상태는 그냥 통과시킴(원래 의도는 "호출부에서 알 진행률을 별도 처리"였는데,
`resumeStoredCompanion()`은 그 별도 처리를 안 하고 있었음) — 그래서 알을
품던 중 보관함에서 다른 개체를 꺼내면 `state.companion`이 그 자리에서 바로
덮어써지면서 품던 알(과 지금까지 쌓인 부화 진행률)이 어디에도 안 남고
사라짐. `boxCurrentEggIfIncubating()`을 추가해서, 지금 알을 품는 중이면
`eggStartTotal`을 그대로 들고 알 보관함으로 돌려보내게 수정(등급은
`guaranteedGrade ?? "common"`). 나중에 그 알을 다시 "품기 시작"하면
진행률이 이어지게, `startIncubatingEgg()`도 `egg.eggStartTotal`이 있으면
그 값에서 이어서 시작하도록 수정.

## 레퍼런스 졸업 후 흐름 확인 — 이미 일치함(코드 변경 없음)

사용자 질문: "레퍼런스에서는 하나 최종까지 다 육성 끝나면 어떻게 되는지".
`CompanionStore.swift`의 `graduate()`를 직접 확인 — `state.active = nil;
state.eggUsage = 0`으로 완전히 새 알(무보증, 등급 알로 부화했어도 보증은
그 부화 시점에 이미 소비됨)로 리셋되고, 토큰은 그 즉시 다시 쌓이기 시작함
(멈춰있지 않음). 우리 `tick()`의 `state.companion = newEgg(totalTokens)`
(졸업 이벤트 뜬 다음 틱)가 정확히 같은 동작이라 코드 변경 없이 확인만 하고
끝남 — 새 랜덤 알 즉시 시작, 키우던 거 재사용 없음, 보관함에서 선택 안 함,
토큰은 계속 쌓임.

## 부스트 기믹(메가진화/기가맥스/테라스탈) — 완료

본가 게임 컨셉(레퍼런스엔 없음)을 딴 사용자 아이디어. 여러 차례 대화로
설계를 다듬은 뒤 구현. 핵심 결정: 부스팅으로 모은 보상을 "그 컴패니언한테
자동 적용"이 아니라 **"이상한 사탕으로 환전해서 기존 가방에 적립"**하는
쪽으로 확정 — 사용자가 "내가 쓰고 싶은 포켓몬한테 쓸 수 있게" 요청해서,
새 아이템 종류를 안 만들고 기존 사탕 인프라를 그대로 재사용하는 쪽으로
설계를 바꿈. 종 제한은 안 둠 — 메가진화 39종/기가맥스 30종/테라스탈(전종)
목록을 조사해뒀지만, 보상이 이미 사탕으로 완전히 추상화돼 있어서 종을
제한하면 대부분의 플레이어가 기능 자체를 못 쓰게 되는 문제가 있어 **지금
키우는 중인 애면 종 무관하게 누구나 부스팅 가능**한 범용 토글로 구현하고,
조사해둔 종 목록은 나중에 시각 연출을 종별로 다르게 하는 코스메틱 레이어용
참고 자료로 `BACKLOG.md`에 남겨둠.

**구현**: `src/boost.js`(신규) — `BOOST_GAUGE_THRESHOLD`(`HATCH_THRESHOLD`의
3배 = 1,500만), `BOOST_BONUS_MULTIPLIER`(1.3배), `candiesFromBoost(amount)`
(반올림 환전). `growth.js`에 `freezeProgress(companion, delta)` 추가 —
부스팅 중엔 매 tick마다 토큰 증가분(delta)만큼 `hatchedAtTotal`도 같이
밀어서 `progress = totalTokens - hatchedAtTotal` 값 자체를 고정시킴(그래서
`evaluate()`가 그 tick만큼은 진행이 안 된 것으로 봐서 부스팅 중엔 자동으로
진화/졸업이 안 터짐 — 별도 분기 불필요). 게이지는 독립된 앵커
(`companion.boostStartTotal`)로 `totalTokens - boostStartTotal`을 실시간
계산해서 판정, 임계치를 넘으면 `tick()`이 즉시 사탕으로 환전+지급하고
부스팅을 해제(해제 후엔 `hatchedAtTotal`이 멈춰있던 값 그대로라 자연스럽게
성장 재개 — "캐치업"으로 따로 당길 필요 없음).

**버그 발견/수정 — 델타를 전체 총량으로 오인하는 첫 틱 버그**: 헤드리스
테스트 중 `progress`가 `-963043320`처럼 깊은 음수로 깨지는 걸 발견함. 원인:
`tick()`의 델타 계산(`totalTokens - lastTotalTokens`)이 앱 시작 직후
첫 `tick()` 호출에서 `lastTotalTokens`의 모듈 초기값(0)을 "직전 tick의 값"
으로 잘못 취급 — 델타가 "이번 틱 증가분"이 아니라 **누적 총량 전체**가
돼버려서, 부스팅 중이던 저장분을 불러왔을 때(또는 그냥 재시작 시점에
우연히 부스팅 중이었을 때) `hatchedAtTotal`이 한 번에 통째로 미래로
밀려버림. `prevTotalTokens > 0`일 때만 델타를 계산하고 첫 tick은 델타 0으로
취급하도록 한 줄 고쳐서 해결 — 재현 테스트로 `progress`가 정상값(예:
시드해둔 500)으로 나오는 것까지 재확인.

**검증**: 헤드리스 Electron으로 (1) 부스팅 중 여러 번 `refresh()`를
돌려도 진행도가 고정돼 있는지, (2) 게이지가 임계치를 넘는 상태로 시드하면
자동 완료돼 `candiesFromBoost()` 계산값 그대로 사탕이 지급되는지(예:
1,700만 모음 → 4개), (3) 게이지 다 차기 전에 수동 취소하면 사탕 지급 없이
꺼지는지, (4) 알(egg) 상태에선 토글이 `not-growing`으로 거부되는지, (5)
실제 팝업 UI에서 버튼 클릭 시 라벨/보조 게이지 바가 올바르게 토글되는지
전부 확인.

## 알 보관함 무한 복제 버그 수정

두 건 연달아 발견/수정:

1. **등급 알 스왑 시 증발**: `startIncubatingEgg()`("알 보관함에서 다른 알로
   갈아타기")가 지금 품던 알을 그냥 버려서, 등급 알 여러 개를 사서 하나씩
   골라 품기 시작할 때마다 이전 알이 사라지고 마지막 알만 남는 버그(사용자가
   실제로 4개 구매 후 겪음). `boxCurrentEggIfIncubating()` 호출을 추가해서
   고침 — 알 보관함에서 하나 꺼내는 것과 정확히 1:1로 맞물려서 안전.
2. **위 수정을 `resumeStoredCompanion()`에도 그대로 적용하려다 발견한 진짜
   문제**: `resumeStoredCompanion()`("보관함 → 다시 키우기")에 같은 함수를
   붙이면, 알 보관함에선 아무것도 안 빠지는데 이 함수가 +1만 하는 구조라
   "보관하고 새 알 시작"(진행도 0짜리 빈 알을 즉시 채워 넣음)과 "다시
   키우기"를 번갈아 누르는 것만으로 실제 토큰 소모 없이 등급 알 티켓이
   무한정 쌓이는 게 확인됨(사용자가 직접 루프를 추적해서 발견: "알 받아.
   보관함→다시키우기→보관하고새알시작→보관함→다시키우기... 알 계속
   늘어나는데?"). `startIncubatingEgg()` 경로는 알 보관함에서 하나 빼는
   것과 1:1로 맞물려 안전한 반면, `resumeStoredCompanion()` 경로는 보관함
   컴패니언을 꺼내올 뿐 알 보관함에선 아무것도 안 빼서 순증가가 생기는
   구조적 차이였음. 해결: `resumeStoredCompanion()`에서 이 호출을 아예
   제거 — 다시 키우기로 갈아탈 때 품던 알(부화 전)은 그냥 버려짐. 진행률
   지키고 싶으면 갈아타기 전에 먼저 부화시키거나, 알 보관함 알끼리의
   1:1 교환만 쓰도록 함(자동 보존 기능 자체를 없애는 게 더 단순하고
   안전하다는 판단, 사용자 확정).

## 레퍼런스 최신 버전 전체 재대조 — 상점/밸런스 상수 구멍 다수 발견

사용자 요청으로 레퍼런스(PokeTokenBar) 최신 소스를 `gh api`로 통째로 다시
확인(`Sources/PokeTokenBar/Core/CompanionModel.swift`,
`CompanionStore.swift`). 예전에 확인했던 `GRADUATION_TOTAL`(레퍼런스 실측
"하루 평균 2.53억 토큰" 기준)은 지금도 정확히 일치하지만(우리 값의 5배),
**상점/보너스 관련 상수는 이번에 처음 제대로 대조됨** — 여러 군데가 어긋나
있었음.

**레퍼런스 최신 상수** (`Sources/PokeTokenBar/Core/CompanionModel.swift`):
- `PokemonBalance.eggHatchThreshold = 5,000,000` (우리와 동일)
- `PokemonBalance.graduationTotal`: common 750M / uncommon 1.875B /
  **rare** 3B / legendary 6B — 등급명 "rare"가 우리의 "epic"에 대응.
  `mythical`은 레퍼런스에 없는 우리 자체 등급(legendary의 2배로 우리가
  추가한 것, 기존에 확인됨)
- `RareCandy.xp = 100,000,000`, `.price = 500,000,000` — **XP의 5배**.
  주석 원문: "토큰이 성장 미터 + 상점 지갑으로 이중 사용되는 구조라, 가격을
  XP와 같게 두면 구매가 사실상 공짜 추가성장이 된다(150M 써서 250M 성장).
  500M로 두면... 실질 보너스 +20%로 억제된다"
- `ShinyCharm.price = 3,000,000,000`(=graduationTotal(rare)와 동급, 우리
  판단과 일치), `.shinyDenominator = 48`(1/64→1/48, +33%). 주석: "×2(1/32)는
  과해 절제"
- `FreshEgg.price = 1,000,000,000`(보증 없는 리롤), 등급 보장 시
  `price × (graduationTotal(tier)/graduationTotal(common))` — 예: rare
  보장 = 1B×4=4B로 **rare 졸업총량(3B)보다 비쌈**(확정 구매 프리미엄).
  `shopTiers = [nil, .uncommon, .rare]`로 **legendary(전설) 보장 알은
  아예 안 팜** — 주석: "최고 등급을 확정 상품으로 만들지 않는다"
- `state.active?.hasGrowthBoost` → 이미 도감에 있는 라인(`collectedFinals`)을
  재부화하면 그 개체는 `repeatGrowthMultiplier = 2`(성장 속도 2배) 적용 —
  중복 개체 좌절감 완화 장치
- 주간 보상(`RareCandy.weeklyGrant = 5`)은 **실제 세션/주간 사용량 한도
  100% 도달**이 트리거(달성 보상). `OAuthLimitsProvider`/
  `SessionKeyLimitsProvider` 등으로 한도 %를 읽어옴 — 우리가 이미
  `BACKLOG.md`에 "OAuth 토큰을 가져다 써야 해서 안 하기로 함"이라고 적어둔
  바로 그 방식이 맞았음(재확인, 판단 변경 없음)

**대조 결과 — 우리 쪽에서 발견된 구멍/역전**:

1. **(구멍, 심각)** `src/shop.js`의 `RARE_CANDY_PRICE = RARE_CANDY_XP`
   (1:1, 500만) — 레퍼런스가 명시적으로 경고한 "가격=XP면 공짜 추가성장"
   함정에 그대로 빠져 있음. 우리 앱도 `spendableTokens`/`progress`가 같은
   `totalTokens`에서 완전히 독립 파생되는 구조(레퍼런스와 동일 원칙으로
   이미 확인된 바 있음 — "상점 + 가방 — 완료" 항목 참고)라 이 함정이
   레퍼런스와 똑같이 적용됨. **사탕이 사실상 무한 무료 부스트** — 누적
   토큰이 많이 쌓인 사용자일수록 그 재화로 사탕을 왕창 사서 한 번에 여러
   단계를 진화/졸업까지 순간이동시킬 수 있음.
2. **(과함)** `SHINY_CHARM_DENOMINATOR = SHINY_DENOMINATOR / 2`
   (1/64→1/32, 정확히 2배) — 레퍼런스는 1/48(+33%)로 의도적으로 완화한
   값을 채택. 크로스체크 없이 "절반"을 직관적으로 넣은 것으로 보임.
3. **(부호 반대)** `eggPrice(tier) = GRADUATION_TOTAL[tier] × 20%` —
   레퍼런스는 등급 보장 알이 그 등급 졸업총량보다 **비싸게**(확정 구매
   프리미엄) 설계돼 있는데, 우리는 **20%로 훨씬 쌈**(예: legendary 240M ≪
   졸업총량 1,200M) — "그냥 사는 게 이득"이라 자연 성장 루프의 의미가
   약해짐.
4. **(설계 누락)** 우리 상점은 `GRADUATION_TOTAL` 전 등급(legendary/
   mythical 포함)을 다 팜 — 레퍼런스의 "최상위 등급은 확정 상품으로 안
   판다" 철학이 반영 안 돼 있음.
5. **(기능 없음)** 재도전 성장 2배 보너스(`repeatGrowthMultiplier`) 자체가
   우리한텐 없음 — 우리는 이미 보유한 종의 뽑힐 확률만 절반으로 깎을 뿐
   (`pickHatchSpecies`), 일단 뽑힌 뒤엔 신규/중복 상관없이 성장 속도가
   완전히 동일함.
6. **(부화 이월 안 됨)** 레퍼런스는 "초과분은 부화체 성장에 이월"이라고
   명시하는데, 우리 `evaluate()`의 hatch 분기는 `hatchedAtTotal:
   totalTokens`로 그 순간의 현재 총량 그대로 리셋해서 임계치 초과분이
   증발함 — egg→hatch 전환 그 순간에만 해당(진화/졸업 단계는
   `hatchedAtTotal`을 안 건드려서 누적 임계치 구조상 초과분이 자연히 잘
   이어짐). 폴링 주기(기본 2분) 안에서는 미미하지만, 앱을 오래 꺼뒀다가
   한 번에 크게 건너뛴 경우("왜 껐다 키면 알 부화해있는 느낌이지" 질문
   참고)엔 손실이 꽤 클 수 있음.

아직 아무것도 고치지 않음(대조·기록만) — 실제로 뭘 어떻게 조정할지는
`BACKLOG.md`의 "레퍼런스 대비 상점/밸런스 조정 후보" 참고.
